# 项目开发约束

本文件适用于本项目及其所有子目录。项目为使用 TypeScript、Vite 和 Canvas 2D 开发的浏览器游戏。

## 纯 Canvas 开发

- 游戏画面及所有可见界面均使用原生 Canvas 2D 绘制，包括场景、角色、特效、文字、图标、状态栏、按钮、菜单、弹窗和可滚动的能力详情。
- 不使用 HTML 元素、DOM 面板、CSS 界面或前端界面框架实现游戏界面，也不引入其他渲染引擎替代 Canvas 2D。
- HTML 和 CSS 仅用于承载 Canvas，以及设置页面基础样式、画布尺寸和屏幕适配。允许使用浏览器原生接口处理输入、音频、资源加载、存储及设备权限。
- 界面交互通过画布坐标转换、命中检测和游戏状态管理实现；缩放后的绘制与交互坐标必须保持一致。
- 后续修改涉及现有非 Canvas 游戏界面时，应将相关界面迁移至 Canvas，不继续扩展 DOM 界面。

## 测试由用户负责

- 在本项目中进行任何修改均无需由开发代理进行测试，所有测试和运行效果验证由用户负责。
- 不主动编写、运行或补跑单元测试、集成测试、浏览器测试、截图验证或其他测试流程，也不以测试为目的执行构建或启动应用。
- 完成修改后简要说明改动，并如实注明未进行测试、由用户测试；不得声称测试已通过。

## 美术素材生成

- 生成角色、道具、图标、特效等独立美术素材时，优先争取真实透明背景，在提示词中明确要求透明背景，并优先使用支持透明通道的输出格式。
- 如果初次生成未得到透明背景，保留该次生成结果，不为获得透明背景而重新生成或重试。
- 初次透明背景生成失败后的抠图由用户负责；开发代理无需自动抠图或另行调用工具去除背景，应说明素材需要用户抠图。
